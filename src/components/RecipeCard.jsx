import { Heart, Clock, ArrowUpRight } from 'lucide-react'
import { Link } from 'react-router-dom'
export default function RecipeCard({ recipe, favorite, onFavorite }) { return <article className="recipe-card"><div className="recipe-image"><img src={recipe.image} alt={recipe.name}/><button className={`heart-btn ${favorite ? 'saved' : ''}`} aria-label={`${favorite ? 'Remove' : 'Save'} ${recipe.name}`} onClick={() => onFavorite(recipe.id)}><Heart size={18} fill={favorite ? 'currentColor' : 'none'}/></button></div><div className="card-body"><div className="recipe-meta"><span>{recipe.category}</span><span><Clock size={14}/> {recipe.time}</span></div><h3>{recipe.name}</h3><p>{recipe.description}</p><div className="recipe-foot"><span>{recipe.calories} kcal</span><Link className="text-button" to={`/recipes/${recipe.id}`}>View recipe <ArrowUpRight size={16}/></Link></div></div></article> }
